package com.shapes.element.presentation.main.keyboard

enum class KeyboardOperator(val symbol: String) {
	Delete("<-"), Open("("), Close(")"), Divide("/"),
	Add("+"), Subtract("-"), Multiply("*"),
	Equal("=")
}
